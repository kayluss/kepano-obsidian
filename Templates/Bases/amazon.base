views:
  - type: table
    name: Table
    filters:
      and:
        - file.hasLink("Amazon Bill")
        - '!file.hasLink("Bills MOC")'
    order:
      - file.name
      - created
    sort:
      - property: created
        direction: DESC
